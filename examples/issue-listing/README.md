# Issue Listing

Lists repository issues through the provider-neutral `issues` capability.

## Run

```sh
pnpm --filter @gitbridge/example-issue-listing build
pnpm --filter @gitbridge/example-issue-listing start
```

Set `GITBRIDGE_GITHUB_TOKEN` and `GITBRIDGE_REPOSITORY_URL` as needed.
