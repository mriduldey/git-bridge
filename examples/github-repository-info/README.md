# GitHub Repository Info

Reads normalized repository metadata through the GitHub provider.

## Run

```sh
pnpm --filter @sourceaxis/example-github-repository-info build
pnpm --filter @sourceaxis/example-github-repository-info start
```

Set `SOURCEAXIS_GITHUB_TOKEN` for authenticated GitHub API access and `SOURCEAXIS_REPOSITORY_URL` to
inspect a different repository.
