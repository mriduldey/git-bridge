# Branch Listing

Lists branches through the provider-neutral `branches` capability.

## Run

```sh
pnpm --filter @gitbridge/example-branch-listing build
pnpm --filter @gitbridge/example-branch-listing start
```

Set `GITBRIDGE_GITHUB_TOKEN` and `GITBRIDGE_REPOSITORY_URL` as needed.
