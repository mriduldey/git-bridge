# @gitbridge/core

Provider-neutral GitBridge runtime foundation.

This package implements the client skeleton, configuration resolution, dependency composition,
provider registration, capability registration, lifecycle management, and immutable runtime context.

It intentionally does not implement repository creation, repository references, provider execution,
or provider-specific behavior.
